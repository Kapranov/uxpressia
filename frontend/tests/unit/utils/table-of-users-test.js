import tableOfUsers from 'frontend/utils/table-of-users';
import { module, test } from 'qunit';

module('Unit | Utility | table of users');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tableOfUsers();
  assert.ok(result);
});
