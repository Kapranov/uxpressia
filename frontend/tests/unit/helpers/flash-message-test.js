
import { flashMessage } from 'frontend/helpers/flash-message';
import { module, test } from 'qunit';

module('Unit | Helper | flash message');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flashMessage([42]);
  assert.ok(result);
});

