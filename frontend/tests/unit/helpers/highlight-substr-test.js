
import { highlightSubstr } from 'frontend/helpers/highlight-substr';
import { module, test } from 'qunit';

module('Unit | Helper | highlight substr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = highlightSubstr([42]);
  assert.ok(result);
});

