
import { makeBold } from 'frontend/helpers/make-bold';
import { module, test } from 'qunit';

module('Unit | Helper | make bold');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeBold([42]);
  assert.ok(result);
});

