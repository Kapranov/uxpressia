
import { dedasherize } from 'frontend/helpers/dedasherize';
import { module, test } from 'qunit';

module('Unit | Helper | dedasherize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dedasherize([42]);
  assert.ok(result);
});

